spring:
  profiles:
    #当前生效环境
    active: dev
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

executor:
  # 核心线程数
  core-poolSize: 4
  # 最大核心数
  maximum-poolSize: 8
  # 超过 corePoolSize 线程数量的线程最大空闲时间
  keep-alive-time: 3
  # 队列长度
  queue-num: 10000


